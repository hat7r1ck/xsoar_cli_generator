<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="color-scheme" content="dark">
    <title>XSOAR Case Tools</title>
    <style>
        /* === Global Styles === */
        body {
            background-color: #000;
            color: #00ff00;
            font-family: 'Courier New', Courier, monospace;
            text-align: center;
            margin: 0;
            padding: 20px;
        }
        h1 {
            text-shadow: 0 0 10px #00ff00;
        }
        .form-container, .tab-content {
            width: 90%;
            max-width: 800px;
            margin: auto;
            text-align: left;
            border: 2px solid #00ff00;
            padding: 20px;
            border-radius: 10px;
            background-color: #111;
            box-shadow: 0 0 15px #00ff00;
        }
        .tab-container {
            display: flex;
            justify-content: center;
            gap: 10px;
            margin-bottom: 20px;
        }
        .tab-button {
            background-color: black;
            color: #00ff00;
            border: 2px solid #00ff00;
            padding: 10px 15px;
            cursor: pointer;
            text-align: center;
        }
        .tab-button:hover {
            background-color: #00ff00;
            color: black;
        }
        .tab-content {
            display: none;
        }
        .active-tab {
            display: block;
        }

        /* === Case Notes Layout === */
        #case-notes {
            display: flex;
            justify-content: space-between;
            gap: 20px;
            align-items: flex-start;
        }
        #notes-left, #notes-right {
            width: 48%;
            padding: 15px;
            border: 2px solid #00ff00;
            box-shadow: 0 0 10px #00ff00;
            border-radius: 8px;
        }
        #notes-left input, #notes-left select, #notes-left textarea {
            width: 100%;
            margin-top: 5px;
            padding: 8px;
            background: black;
            color: #00ff00;
            border: 1px solid #00ff00;
            box-sizing: border-box;
        }
        #notes-left textarea {
            resize: vertical;
            height: 50px;
        }
        #lookback-container {
            border: 2px solid #00ff00;
            padding: 10px;
            background: #111;
            margin-top: 10px;
        }
        .lookback-item {
            display: flex;
            align-items: center;
            gap: 10px;
            margin-bottom: 5px;
        }
        .lookback-item label {
            flex: 1;
        }
        .lookback-item input[type="checkbox"] {
            transform: scale(1.2);
            accent-color: #00ff00;
        }
        #notes-output {
            border: 2px solid #00ff00;
            background: #111;
            color: #00ff00;
            white-space: pre-wrap;
            overflow-y: auto;
            height: 400px;
            padding: 10px;
            border-radius: 8px;
        }
        .button-container {
            display: flex;
            justify-content: space-between;
            margin-top: 15px;
        }
        .button-container button {
            width: 48%;
            padding: 10px;
            background: black;
            color: #00ff00;
            border: 2px solid #00ff00;
            cursor: pointer;
            font-weight: bold;
            box-shadow: 0 0 5px #00ff00;
        }
        .button-container button:hover {
            background: #00ff00;
            color: black;
        }
    </style>
</head>
<body>

    <h1>XSOAR Case Tools</h1>

    <div class="tab-container">
        <button class="tab-button" onclick="showTab('cli-generator')">CLI Generator</button>
        <button class="tab-button" onclick="showTab('case-notes')">Case Notes</button>
    </div>

    <!-- CLI Generator -->
    <div id="cli-generator" class="tab-content active-tab">
        <h2>CLI Generator</h2>
        <div class="form-container">
            <label for="occurred-time">Occurred Time (UTC):</label>
            <input type="datetime-local" id="occurred-time">
            <button onclick="generateCLI()">Generate</button>
            <pre id="cli-output"></pre>
            <button onclick="copyCLI()">Copy</button>
        </div>
    </div>

    <!-- Case Notes Generator -->
    <div id="case-notes" class="tab-content">
        <h2>Case Notes Generator</h2>
        <div id="notes-left">
            <label for="detection-time">Detection Time:</label>
            <input type="text" id="detection-time" oninput="generateNotes()">

            <label for="username">Username:</label>
            <input type="text" id="username" oninput="generateNotes()">

            <label for="hostname">Host Name:</label>
            <input type="text" id="hostname" oninput="generateNotes()">

            <label for="detection-name">Detection Name:</label>
            <input type="text" id="detection-name" oninput="generateNotes()">

            <h4>Lookback</h4>
            <div id="lookback-container">
                <div class="lookback-item">
                    <input type="checkbox" id="checked-cases" onchange="generateNotes()">
                    <label for="checked-cases">Checked prior cases</label>
                </div>
                <div class="lookback-item">
                    <input type="checkbox" id="checked-hosts" onchange="generateNotes()">
                    <label for="checked-hosts">Checked prior hosts</label>
                </div>
            </div>

            <div class="button-container">
                <button onclick="copyNotes()">Copy Notes</button>
                <button onclick="resetNotes()">Reset</button>
            </div>
        </div>

        <div id="notes-right">
            <h3>Generated Case Notes</h3>
            <pre id="notes-output"></pre>
        </div>
    </div>

    <script>
        function showTab(tabId) {
            document.querySelectorAll(".tab-content").forEach(tab => tab.classList.remove("active-tab"));
            document.getElementById(tabId).classList.add("active-tab");
        }

        function generateNotes() {
            let detectionTime = document.getElementById("detection-time").value.trim();
            let username = document.getElementById("username").value.trim();
            let hostname = document.getElementById("hostname").value.trim();
            let detectionName = document.getElementById("detection-name").value.trim();

            let md = `
On **\`${detectionTime}\`**, XSOAR detected activity for **\`${username}\`** on **\`${hostname}\`**.

Detection Name: **${detectionName}**
            `.trim();

            document.getElementById("notes-output").textContent = md;
        }

        function copyNotes() {
            navigator.clipboard.writeText(document.getElementById("notes-output").textContent);
        }

        function resetNotes() {
            document.querySelectorAll("#case-notes input").forEach(el => el.value = "");
            document.getElementById("notes-output").textContent = "";
        }
    </script>
</body>
</html>
