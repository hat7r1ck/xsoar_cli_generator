/* Case Notes Section */
.case-notes-container {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    width: 100%;
    gap: 20px;
}

/* Ensure the left section has proper width */
.case-notes-inputs {
    flex: 1;
    border: 2px solid #00ff00;
    padding: 15px;
    background-color: #111;
    box-shadow: 0 0 15px #00ff00;
}

/* Right side - Generated Case Notes Output */
.case-notes-output {
    flex: 1;
    border: 2px solid #00ff00;
    padding: 15px;
    background-color: #111;
    box-shadow: 0 0 15px #00ff00;
    height: auto;
    min-height: 200px;
    max-height: 300px;
    overflow-y: auto;
}

/* Checkboxes - Aligned */
.checkbox-container {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    border: 2px solid #00ff00;
    padding: 10px;
    margin-top: 10px;
    background-color: #111;
}

/* Checkbox Item - Aligned Properly */
.checkbox-item {
    display: flex;
    align-items: center;
    margin-bottom: 5px;
}

/* Reduce Checkbox Size */
.checkbox-item input {
    width: 16px;
    height: 16px;
    margin-right: 8px;
}

/* Divider between checkboxes */
.checkbox-item:not(:last-child) {
    border-bottom: 1px solid #00ff00;
    padding-bottom: 5px;
    margin-bottom: 5px;
}

/* Ensure the Notes Output matches the size of input */
#case-notes-output {
    width: 100%;
    height: 200px;
    background-color: #111;
    color: #00ff00;
    border: 2px solid #00ff00;
    padding: 10px;
    white-space: pre-wrap;
}

<div class="case-notes-container">
    <!-- Case Notes Inputs -->
    <div class="case-notes-inputs">
        <h2>Case Notes Generator</h2>
        
        <label for="detection-time">Detection Time:</label>
        <input type="text" id="detection-time">

        <label for="username">Username:</label>
        <input type="text" id="username">

        <label for="hostname">Host Name:</label>
        <input type="text" id="hostname">

        <label for="detection-name">Detection Name:</label>
        <input type="text" id="detection-name">

        <h4>Lookback</h4>
        <div class="checkbox-container">
            <div class="checkbox-item">
                <input type="checkbox" id="checked-cases">
                <label for="checked-cases">Checked prior cases</label>
            </div>
            <div class="checkbox-item">
                <input type="checkbox" id="checked-hosts">
                <label for="checked-hosts">Checked prior hosts</label>
            </div>
            <div class="checkbox-item">
                <input type="checkbox" id="checked-ioc">
                <label for="checked-ioc">Checked prior IoCs</label>
            </div>
        </div>

        <button onclick="copyNotes()">Copy Notes</button>
        <button onclick="resetCaseNotes()">Reset</button>
    </div>

    <!-- Case Notes Output -->
    <div class="case-notes-output">
        <h2>Generated Case Notes</h2>
        <div id="case-notes-output"></div>
    </div>
</div>
